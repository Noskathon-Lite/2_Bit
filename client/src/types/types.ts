export interface UserType {
  _id: string;
  name: string;
  email: string;
  address: string;
  role: string; // Example: 'organization', 'admin', etc.
  status: string; // Example: 'active', 'inactive', etc.
}
